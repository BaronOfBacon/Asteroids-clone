using Core;

namespace Asteroids.Weapon.Projectile
{
    public class ProjectilePresenter : Presenter<ProjectileModel, ProjectileView>
    {
        public ProjectilePresenter(ProjectileModel model, ProjectileView view) : base(model, view)
        {
            
        }
        
    }
}
